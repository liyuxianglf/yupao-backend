# 线上配置文件
# 注意开源时这个文件不要提交、或者不要填真实配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yupao?useSSL=false
    username: root
    password: yuxi@sql666
  redis:
    port: 6380
    host: 127.0.0.1
    password: yuxi666@@
    database: 1
server:
  address: 0.0.0.0
  port: 8080
  servlet:
    context-path: /api
    session:
      cookie:
        domain: 101.42.24.244
        same-site: lax
        secure: false
        http-only: true


